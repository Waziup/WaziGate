version: '3'
services:

#-------------------------#

  wazigate-system:
    build: ./wazigate-system
    volumes:
      - ./wazigate-system:/app
    environment:
      - PYTHONUNBUFFERED=TRUE

#-------------------------#

  wazigate-ui:
    build: ./wazigate-ui
    volumes:
      - ./wazigate-ui:/var/www/html
    environment:
      - DEBUG_MODE=1


#-------------------------#

  wazigate-edge:
    build: ./wazigate-edge

#-------------------------#


#-------------------------#
